class Solution {
  public:
    int maxSum(vector<int> &arr) {
        
        int maxi = INT_MIN;
        
        int n = arr.size();
        
        for(int i=0; i<n-1; i++)
        {
            maxi = max(maxi,arr[i]+arr[i+1]);
        }
        
        return maxi;
    }
};
